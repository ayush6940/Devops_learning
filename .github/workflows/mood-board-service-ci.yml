name: Mood Board Service CI

on:
  push:
    branches: [ "main" ]
    paths:
      - 'mood-board-service/**'
      - '.github/workflows/mood-board-service-ci.yml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'mood-board-service/**'
      - '.github/workflows/mood-board-service-ci.yml'

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./mood-board-service

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    - name: Build and verify with Maven
      run: mvn clean verify